{
  "name": "asqav-activepieces",
  "version": "0.1.0",
  "description": "Activepieces piece for Asqav. Signs an action with an Asqav agent and returns the signed receipt.",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --experimental-strip-types --experimental-test-module-mocks --test \"test/**/*.test.ts\""
  },
  "dependencies": {
    "@activepieces/pieces-framework": "0.28.2",
    "@asqav/sdk": "0.5.3"
  },
  "devDependencies": {
    "typescript": "5.5.4"
  }
}
